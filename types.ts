export interface SeriesPost {
  id: string;
  title: string;
  imageUrl: string;
  price: string;
  description: string;
}

export interface ContactConfig {
  phoneNumber: string; // e.g. 543512417464
  whatsappMessage: string;
}